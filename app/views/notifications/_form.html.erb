<%= simple_form_for(@notification) do |f| %>
  <%= f.error_notification %>
  <p>Fill in the following information, and we'll notify you when StreetSeen is ready 
  to use!  StreetSeen is expected to be available in May.</p>
  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :email %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
