<%# image_tag("http://maps.googleapis.com/maps/api/streetview?size=" + @width.to_s + "x" + @height.to_s + "&location=" + @location.latitude.to_s() + "," + @location.longitude.to_s() + "&pitch=" + @location.pitch.to_s + "&heading" + @location.heading.to_s + "&sensor=false", :size => @width.to_s + "x" + @height.to_s) %>
