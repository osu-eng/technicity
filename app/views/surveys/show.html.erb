<%= simple_form_for @surveyForm, url: survey_path,
                    method: :post, as: :survey, html: {class: 'form-horizontal'} do |f| %>
    <%= render 'shared/error_messages', object: f.object %>
    <%= @survey.description %>




<% end %>
